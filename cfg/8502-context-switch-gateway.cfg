# SPDX-License-Identifier: GPL-3.0-or-later
# Context-switch spike core: common RAM below the MMU register page, clear of
# the $F180 result block.

MEMORY {
    COMMON: start = $F400, size = $0B00, type = ro, file = %O;
}

SEGMENTS {
    CODE: load = COMMON, type = ro;
}
