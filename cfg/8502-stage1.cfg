# SPDX-License-Identifier: GPL-3.0-or-later

MEMORY {
    LOADER: start = $1C00, size = $0400, type = ro, file = %O, fill = yes;
}

SEGMENTS {
    CODE: load = LOADER, type = ro;
    SPRITE: load = LOADER, type = ro, start = $1FC0;
}
