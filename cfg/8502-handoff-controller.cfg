# SPDX-License-Identifier: GPL-3.0-or-later

MEMORY {
    MAIN: start = $2800, size = $0800, type = ro, file = %O,
          fill = yes, fillval = $EA;
}

SEGMENTS {
    CODE: load = MAIN, type = ro;
}
