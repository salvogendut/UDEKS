# SPDX-License-Identifier: GPL-3.0-or-later
# Temporary 8502 shim followed immediately by the Z80 payload.

MEMORY {
    MAIN: start = $1FE0, size = $2020, type = ro, file = %O;
}

SEGMENTS {
    CODE: load = MAIN, type = ro;
}
